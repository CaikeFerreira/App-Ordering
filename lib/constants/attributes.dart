import 'package:intl/intl.dart';

const defaultPadding = 8.0;
const double defaultRadiusBorder = 5;

const locale = "pt-BR";

const fontSize = 12;

const dataFormat = "dd/MM/yyyy HH:mm";

NumberFormat formatMoeda = NumberFormat.simpleCurrency(locale: locale);
